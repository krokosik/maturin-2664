# This file is autogenerated by maturin v1.9.0
# To update, run
#
#    maturin generate-ci github --platform linux windows -o ../build.yml
#
name: CI

on:
  push:
    branches:
      - main
      - master
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Install uv
        uses: astral-sh/setup-uv@v5
      - name: Run develop
        working-directory: host
        run: uv run maturin develop
      - name: Check if LICENSE file was deleted
        run: test -f LICENSE
        continue-on-error: true

  windows:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Install uv
        uses: astral-sh/setup-uv@v5
      - name: Run develop
        working-directory: host
        run: uv run maturin develop
      - name: Check if LICENSE file was deleted
        shell: bash
        run: test -f LICENSE
        continue-on-error: true

